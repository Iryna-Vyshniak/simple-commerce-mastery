const Title = ({ children }) => {
  return (
    <h2 className="mt-10 font-palanquin text-3xl max-sm:text-[32px] max-sm:leading-[22px] font-bold text-left">
      {children}
    </h2>
  );
};

export default Title;
